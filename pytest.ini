[pytest]
markers =
    unit: Unit tests — no HTTP, no LLM, mocked HTML only
    integration: Integration tests — hits live URLs, requires network
    llm: LLM quality tests — requires GEMINI_API_KEY or Ollama
